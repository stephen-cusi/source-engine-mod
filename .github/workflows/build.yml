name: Build

on:
  push:
    branches:
      - "**"
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ---------------------------------------------------------------------------
  # Linux
  # ---------------------------------------------------------------------------

  build-linux-x86_64:
    name: Linux (x86_64)
    runs-on: ubuntu-24.04
    timeout-minutes: 60

    steps:
      - name: Check out source
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y \
            build-essential \
            libavcodec-dev \
            libavformat-dev \
            libavutil-dev \
            libbz2-dev \
            libcurl4-gnutls-dev \
            libedit-dev \
            libfontconfig1-dev \
            libfreetype6-dev \
            libjpeg-dev \
            libopenal-dev \
            libpng-dev \
            libsdl2-dev \
            libswresample-dev \
            zlib1g-dev

      - name: Configure
        run: >-
          python3 ./waf configure
          -T release
          --disable-warns
          --prefix="${{ github.workspace }}/linux"
          --build-games=hl2

      - name: Build
        run: python3 ./waf build -j2

      - name: Install
        run: python3 ./waf install -j2

      - name: Save build configuration
        run: cp build/config.log linux/info.txt

      - name: Upload Linux build
        uses: actions/upload-artifact@v7
        with:
          name: source-engine-linux-x86_64
          path: linux
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # Android
  # ---------------------------------------------------------------------------

  build-android-armv7a:
    name: Android (armeabi-v7a)
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Check out source
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -f -y libopenal-dev g++-multilib gcc-multilib libpng-dev libjpeg-dev libfreetype6-dev libfontconfig1-dev libcurl4-gnutls-dev libsdl2-dev zlib1g-dev libbz2-dev libedit-dev

      - name: Download Android NDK and toolchain
        run: |
          wget https://dl.google.com/android/repository/android-ndk-r10e-linux-x86_64.zip -nc -q
          wget https://github.com/llvm/llvm-project/releases/download/llvmorg-11.1.0/clang+llvm-11.1.0-x86_64-linux-gnu-ubuntu-16.04.tar.xz -nc -q
          unzip -q android-ndk-r10e-linux-x86_64.zip
          tar -xf clang+llvm-11.1.0-x86_64-linux-gnu-ubuntu-16.04.tar.xz

      - name: Configure
        run: |
          export ANDROID_NDK_HOME="${{ github.workspace }}/android-ndk-r10e/"
          export PATH="${{ github.workspace }}/clang+llvm-11.1.0-x86_64-linux-gnu-ubuntu-16.04/bin:$PATH"
          ./scripts/build-ffmpeg-android.sh armeabi-v7a-hard
          python3 ./waf configure \
            -T release \
            --prefix=../android_build \
            --android=armeabi-v7a-hard,host,21 \
            --target=../android_build/armeabi-v7a \
            --disable-warns \
            -4 \
            --togles

      - name: Build and install
        run: |
          export ANDROID_NDK_HOME="${{ github.workspace }}/android-ndk-r10e/"
          export PATH="${{ github.workspace }}/clang+llvm-11.1.0-x86_64-linux-gnu-ubuntu-16.04/bin:$PATH"
          python3 ./waf install --strip

      - name: Upload Android build
        uses: actions/upload-artifact@v7
        with:
          name: build-android-armv7a
          path: /home/runner/work/source-engine-mod/android_build/lib/armeabi-v7a/*.*
          if-no-files-found: error

  build-android-aarch64:
    name: Android (arm64-v8a)
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Check out source
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -f -y libopenal-dev g++-multilib gcc-multilib libpng-dev libjpeg-dev libfreetype6-dev libfontconfig1-dev libcurl4-gnutls-dev libsdl2-dev zlib1g-dev libbz2-dev libedit-dev

      - name: Download Android NDK and toolchain
        run: |
          wget https://dl.google.com/android/repository/android-ndk-r10e-linux-x86_64.zip -nc -q
          wget https://github.com/llvm/llvm-project/releases/download/llvmorg-11.1.0/clang+llvm-11.1.0-x86_64-linux-gnu-ubuntu-16.04.tar.xz -nc -q
          unzip -q android-ndk-r10e-linux-x86_64.zip
          tar -xf clang+llvm-11.1.0-x86_64-linux-gnu-ubuntu-16.04.tar.xz

      - name: Configure
        run: |
          export ANDROID_NDK_HOME="${{ github.workspace }}/android-ndk-r10e/"
          export PATH="${{ github.workspace }}/clang+llvm-11.1.0-x86_64-linux-gnu-ubuntu-16.04/bin:$PATH"
          ./scripts/build-ffmpeg-android.sh aarch64
          python3 ./waf configure \
            -T release \
            --prefix=../android_build \
            --android=aarch64,host,21 \
            --target=../android_build/aarch64 \
            --disable-warns \
            --togles

      - name: Build and install
        run: |
          export ANDROID_NDK_HOME="${{ github.workspace }}/android-ndk-r10e/"
          export PATH="${{ github.workspace }}/clang+llvm-11.1.0-x86_64-linux-gnu-ubuntu-16.04/bin:$PATH"
          python3 ./waf install --strip

      - name: Upload Android build
        uses: actions/upload-artifact@v7
        with:
          name: build-android-aarch64
          path: /home/runner/work/source-engine-mod/android_build/lib/arm64-v8a/*.*
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # Windows
  # ---------------------------------------------------------------------------

  build-windows-i386:
    name: Windows (x86)
    runs-on: windows-2022
    timeout-minutes: 60

    steps:
      - name: Check out source
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Configure
        run: >-
          ./waf.bat configure
          -T release
          --prefix=./win
          --disable-warns
          --32bits

      - name: Build and install
        run: ./waf.bat install

      - name: Cleanup artifacts (remove .lib .pdb .obj .exp .ilk)
        run: |
          Get-ChildItem -Path ./win -Recurse -Include *.lib,*.pdb,*.obj,*.exp,*.ilk | Remove-Item -Force -Recurse

      - name: Upload Windows build
        uses: actions/upload-artifact@v7
        with:
          name: win-source-engine-i386
          path: ./win
          if-no-files-found: error

  build-windows-amd64:
    name: Windows (x64)
    runs-on: windows-2022
    timeout-minutes: 60

    steps:
      - name: Check out source
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Prepare FFmpeg
        shell: pwsh
        run: |
          curl -sL -o ffmpeg.zip https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-n8.1-latest-win64-gpl-shared-8.1.zip
          Expand-Archive -Force ffmpeg.zip -DestinationPath ffmpeg
          $ff = (Get-ChildItem ffmpeg -Directory | Select-Object -First 1).FullName
          Write-Output "FFmpeg dir: $ff"
          Copy-Item "$ff/include/libavcodec","$ff/include/libavdevice","$ff/include/libavfilter","$ff/include/libavformat","$ff/include/libavutil" common/ -Recurse -Force
          Copy-Item "$ff/include/libswresample","$ff/include/libswscale" common/ -Recurse -Force
          Copy-Item "$ff/lib/avcodec.lib","$ff/lib/avformat.lib","$ff/lib/avutil.lib","$ff/lib/swresample.lib","$ff/lib/swscale.lib" lib/win32/amd64/
          Copy-Item "$ff/bin/avcodec-62.dll","$ff/bin/avformat-62.dll","$ff/bin/avutil-60.dll","$ff/bin/swresample-6.dll","$ff/bin/swscale-9.dll" lib/win32/amd64/

      - name: Configure
        shell: pwsh
        run: >-
          ./waf.bat configure
          -T release
          --prefix=./win
          --disable-warns

      - name: Build and install
        shell: pwsh
        run: ./waf.bat install

      - name: Cleanup artifacts (remove .lib .pdb .obj .exp .ilk)
        run: |
          Get-ChildItem -Path ./win -Recurse -Include *.lib,*.pdb,*.obj,*.exp,*.ilk | Remove-Item -Force -Recurse

      - name: Upload Windows build
        uses: actions/upload-artifact@v7
        with:
          name: win-source-engine-amd64
          path: ./win
          if-no-files-found: error

  build-dedicated-windows-i386:
    name: Windows Dedicated (x86)
    runs-on: windows-2022
    timeout-minutes: 60

    steps:
      - name: Check out source
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Configure
        run: >-
          ./waf.bat configure
          -T release
          -d
          --prefix=./win
          -4

      - name: Build and install
        run: ./waf.bat install

      - name: Cleanup artifacts (remove .lib .pdb .obj .exp .ilk)
        run: |
          Get-ChildItem -Path ./win -Recurse -Include *.lib,*.pdb,*.obj,*.exp,*.ilk | Remove-Item -Force -Recurse

      - name: Upload Windows build
        uses: actions/upload-artifact@v7
        with:
          name: win-source-engine-d-i386
          path: ./win
          if-no-files-found: error

  build-dedicated-windows-amd64:
    name: Windows Dedicated (x64)
    runs-on: windows-2022
    timeout-minutes: 60

    steps:
      - name: Check out source
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Prepare FFmpeg
        shell: pwsh
        run: |
          curl -sL -o ffmpeg.zip https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-n8.1-latest-win64-gpl-shared-8.1.zip
          Expand-Archive -Force ffmpeg.zip -DestinationPath ffmpeg
          $ff = (Get-ChildItem ffmpeg -Directory | Select-Object -First 1).FullName
          Write-Output "FFmpeg dir: $ff"
          Copy-Item "$ff/include/libavcodec","$ff/include/libavdevice","$ff/include/libavfilter","$ff/include/libavformat","$ff/include/libavutil" common/ -Recurse -Force
          Copy-Item "$ff/include/libswresample","$ff/include/libswscale" common/ -Recurse -Force
          Copy-Item "$ff/lib/avcodec.lib","$ff/lib/avformat.lib","$ff/lib/avutil.lib","$ff/lib/swresample.lib","$ff/lib/swscale.lib" lib/win32/amd64/
          Copy-Item "$ff/bin/avcodec-62.dll","$ff/bin/avformat-62.dll","$ff/bin/avutil-60.dll","$ff/bin/swresample-6.dll","$ff/bin/swscale-9.dll" lib/win32/amd64/

      - name: Configure
        shell: pwsh
        run: >-
          ./waf.bat configure
          -T release
          -d
          --prefix=./win
          --disable-warns

      - name: Build and install
        shell: pwsh
        run: ./waf.bat install

      - name: Cleanup artifacts (remove .lib .pdb .obj .exp .ilk)
        run: |
          Get-ChildItem -Path ./win -Recurse -Include *.lib,*.pdb,*.obj,*.exp,*.ilk | Remove-Item -Force -Recurse

      - name: Upload Windows build
        uses: actions/upload-artifact@v7
        with:
          name: win-source-engine-d-amd64
          path: ./win
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # macOS
  # ---------------------------------------------------------------------------

  build-macos-amd64:
    name: macOS (x86_64)
    runs-on: macos-15-intel
    timeout-minutes: 60

    steps:
      - name: Check out source
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Install build dependencies
        run: |
          brew untap aws/tap || true
          brew install sdl2 pkg-config ffmpeg

      - name: Configure
        run: >-
          python3 ./waf configure
          -T release
          --disable-warns
          --prefix=./macos

      - name: Build and install
        run: |
          python3 ./waf build
          python3 ./waf install

      - name: Upload macOS build
        uses: actions/upload-artifact@v7
        with:
          name: source-engine-macos-amd64
          path: ./macos
          if-no-files-found: error

  build-macos-arm64:
    name: macOS (arm64)
    runs-on: macos-latest
    timeout-minutes: 60

    steps:
      - name: Check out source
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Install build dependencies
        run: |
          brew untap aws/tap || true
          brew install sdl2 pkg-config ffmpeg

      - name: Configure
        env:
          MACHINE: ${{ runner.arch }}
        run: |
          if [ "$(uname -m)" != "arm64" ]; then
            echo "Expected arm64 host, got: $(uname -m)" >&2
            exit 1
          fi
          if [ "$(python3 -c 'import platform; print(platform.machine())')" != "arm64" ]; then
            echo "Python reports non-arm64 (likely running under Rosetta); native Python required" >&2
            exit 1
          fi
          python3 ./waf configure -T release --disable-warns --prefix=./macos

      - name: Build and install
        run: |
          python3 ./waf build
          python3 ./waf install

      - name: Upload macOS build
        uses: actions/upload-artifact@v7
        with:
          name: source-engine-macos-arm64
          path: ./macos
          if-no-files-found: error

  build-dedicated-macos-amd64:
    name: macOS Dedicated (x86_64)
    runs-on: macos-15-intel
    timeout-minutes: 60

    steps:
      - name: Check out source
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Install build dependencies
        run: |
          brew untap aws/tap || true
          brew install sdl2 pkg-config ffmpeg

      - name: Configure
        run: >-
          python3 ./waf configure
          -T release
          -d
          --disable-warns
          --prefix=./macos

      - name: Build and install
        run: |
          python3 ./waf build
          python3 ./waf install

      - name: Upload macOS build
        uses: actions/upload-artifact@v7
        with:
          name: source-engine-dedicated-macos-amd64
          path: ./macos
          if-no-files-found: error

  build-dedicated-macos-arm64:
    name: macOS Dedicated (arm64)
    runs-on: macos-latest
    timeout-minutes: 60

    steps:
      - name: Check out source
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Install build dependencies
        run: |
          brew untap aws/tap || true
          brew install sdl2 pkg-config ffmpeg

      - name: Configure
        run: |
          if [ "$(uname -m)" != "arm64" ]; then
            echo "Expected arm64 host, got: $(uname -m)" >&2
            exit 1
          fi
          if [ "$(python3 -c 'import platform; print(platform.machine())')" != "arm64" ]; then
            echo "Python reports non-arm64 (likely running under Rosetta); native Python required" >&2
            exit 1
          fi
          python3 ./waf configure -T release -d --disable-warns --prefix=./macos

      - name: Build and install
        run: |
          python3 ./waf build
          python3 ./waf install

      - name: Upload macOS build
        uses: actions/upload-artifact@v7
        with:
          name: source-engine-dedicated-macos-arm64
          path: ./macos
          if-no-files-found: error
